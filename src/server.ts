import express from 'express';
import type Database from 'better-sqlite3';
import { randomUUID } from 'node:crypto';
import { validateMonitor, type MonitorConfig } from './config.js';
import { backupDatabase, dashboardData, getMonitor, heartbeatUrl, notificationHistory, receiveHeartbeat, removeMonitor, saveMonitor, setMaintenance, setMonitorActive, setNotificationConfig } from './db.js';
import type { CheckResult } from './check.js';

const esc=(v:unknown)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const baseCss='<style>body{font-family:system-ui;max-width:1100px;margin:auto;padding:24px;background:#f5f7f9;color:#17212b}.panel,.card,.stat{background:#fff;border:1px solid #ddd;border-radius:10px;padding:16px}.cards{display:grid;gap:12px}.group{margin:20px 0}.row{display:flex;justify-content:space-between;gap:12px}.url,.muted{color:#64748b;font-size:13px}.badge{background:#dcfce7;color:#166534;border-radius:20px;padding:5px 9px;font-size:12px}.badge.down{background:#fee2e2;color:#991b1b}.metrics{display:flex;gap:20px;flex-wrap:wrap;margin:14px 0;font-size:13px}.metrics strong{display:block;font-size:15px}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:14px}button{padding:7px 10px;border:1px solid #cbd5e1;border-radius:6px;background:#fff}input,select,textarea{box-sizing:border-box;width:100%;padding:8px;margin-top:4px;border:1px solid #cbd5e1;border-radius:6px}form{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.wide{grid-column:span 2}@media(max-width:650px){form{grid-template-columns:1fr}.wide{grid-column:auto}}</style>';
function card(m:any){const domain=m.type==='domain';const hb=m.type==='heartbeat';return `<article class="card"><div class="row"><div><strong>${esc(m.name)}</strong>${m.group?` <small>(${esc(m.group)})</small>`:''}<div class="url">${esc(m.url)}</div></div><span class="badge ${m.state==='DOWN'?'down':''}">${m.maintenance?'MAINTENANCE':m.state}</span></div><div class="metrics"><div>24h<strong>${m.uptime24h.toFixed(2)}%</strong></div><div>7d<strong>${m.uptime7d.toFixed(2)}%</strong></div><div>30d<strong>${m.uptime30d.toFixed(2)}%</strong></div><div>${domain?'Days remaining':hb?'Last heartbeat':'Latency'}<strong>${domain?(m.domainDaysRemaining??'Unknown'):hb?(m.lastHeartbeatAt?new Date(m.lastHeartbeatAt).toLocaleString():'Never'):(m.latestLatency==null?'N/A':m.latestLatency+' ms')}</strong></div><div>Type<strong>${esc(m.type)}</strong></div></div>${domain?`<p class="muted">Expires: ${m.domainExpirationDate?new Date(m.domainExpirationDate).toLocaleDateString():'Unknown'} · Warning threshold: ${m.warningThreshold} days</p>`:''}${hb&&m.heartbeatUrl?`<p><input readonly value="${esc(m.heartbeatUrl)}"><button data-copy="${esc(m.heartbeatUrl)}">Copy heartbeat URL</button></p>`:''}<p class="muted">${m.incidents.length} incident(s)</p></article>`;}
function publicPage(data:any[]){const groups=new Map<string,any[]>();data.forEach(m=>groups.set(m.group||'Ungrouped',[...(groups.get(m.group||'Ungrouped')||[]),m]));return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Status</title>${baseCss}</head><body><h1>${data.some(m=>m.state==='DOWN')?'Some systems are down':'All systems operational'}</h1>${[...groups].map(([g,ms])=>`<section class="group"><h2>${esc(g)}</h2><div class="cards">${ms.map(card).join('')}</div></section>`).join('')}</body></html>`;}
function adminPage(){return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Dashboard</title>${baseCss}</head><body><h1>Monitor dashboard</h1><p><a href="/status">Public status</a> · <a href="/api/admin/export.csv">CSV export</a></p><section class="panel"><form id="form"><input id="id" type="hidden"><label>Name<input id="name" required></label><label class="wide">Domain or URL<input id="url"></label><label>Type<select id="type"><option>http</option><option>heartbeat</option><option>domain</option><option>ssl</option><option>tcp</option><option>dns</option><option>ping</option><option>websocket</option></select></label><label>Group<input id="group"></label><label>Method<select id="method"><option>GET</option><option>POST</option><option>PUT</option><option>PATCH</option><option>HEAD</option></select></label><label>Interval<input id="interval" type="number" min="5" value="60"></label><label>Timeout<input id="timeout" type="number" min="100" value="10000"></label><label>Expected status<input id="expectedStatus" type="number" value="200"></label><label>Expected heartbeat seconds<input id="expectedInterval" type="number" min="1" value="60"></label><label>Grace period seconds<input id="gracePeriod" type="number" min="0" value="30"></label><label>Warning threshold<select id="warningThreshold"><option>30</option><option>14</option><option>7</option></select></label><label>Keyword<input id="keyword"></label><label>Max latency<input id="maxLatency" type="number" min="1"></label><label class="wide">Headers JSON<input id="headers"></label><label class="wide">Body<textarea id="body"></textarea></label><label>JSON path<input id="jsonPath"></label><label>Expected JSON value<input id="jsonExpected"></label><button>Save monitor</button></form><p id="message" class="muted"></p></section><main id="list"></main><script>${adminScript()}</script></body></html>`;}
function adminScript(){return `const $=id=>document.getElementById(id);let monitors=[];const fields=['id','name','url','type','group','method','interval','timeout','expectedStatus','expectedInterval','gracePeriod','warningThreshold','keyword','maxLatency','headers','body','jsonPath','jsonExpected'];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){monitors=await (await fetch('/api/admin/monitors',{cache:'no-store'})).json();$('list').innerHTML=monitors.map(m=>'<article class="card"><div class="row"><b>'+esc(m.name)+'</b><span class="badge '+(m.state==='DOWN'?'down':'')+'">'+(m.maintenance?'MAINTENANCE':m.state)+'</span></div><div class="url">'+esc(m.url)+'</div><div class="metrics"><div>Type<strong>'+esc(m.type)+'</strong></div><div>24h<strong>'+m.uptime24h.toFixed(2)+'%</strong></div><div>'+(m.type==='domain'?'Days remaining':'Latency')+'<strong>'+(m.type==='domain'?(m.domainDaysRemaining??'Unknown'):(m.latestLatency??'N/A'))+'</strong></div></div><div class="actions"><button data-a="edit" data-id="'+m.id+'">Edit</button><button data-a="delete" data-id="'+m.id+'">Delete</button></div></article>').join('')}function edit(id){const m=monitors.find(x=>x.id===id);fields.forEach(k=>$(k).value=k==='headers'&&m.headers?JSON.stringify(m.headers):m[k]??(k==='expectedInterval'?60:k==='warningThreshold'?30:''));}document.getElementById('form').onsubmit=async e=>{e.preventDefault();let headers;try{headers=$('headers').value?JSON.parse($('headers').value):undefined}catch{$('message').textContent='Headers must be valid JSON';return}const hb=$('type').value==='heartbeat',domain=$('type').value==='domain';const data={name:$('name').value,url:hb?'heartbeat://pending':domain?$ ('url').value:$('url').value,type:$('type').value,group:$('group').value||undefined,method:$('method').value,interval:hb?5:+$('interval').value,timeout:hb?1000:+$('timeout').value,expectedStatus:+$('expectedStatus').value,expectedInterval:hb?+$('expectedInterval').value:undefined,gracePeriod:hb?+$('gracePeriod').value:undefined,warningThreshold:domain?+$('warningThreshold').value:undefined,keyword:$('keyword').value||undefined,maxLatency:$('maxLatency').value?+$('maxLatency').value:undefined,headers,body:$('body').value||undefined,jsonPath:$('jsonPath').value||undefined,jsonExpected:$('jsonExpected').value||undefined};const id=$('id').value;const r=await fetch('/api/admin/monitors'+(id?'/'+id:''),{method:id?'PUT':'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});if(!r.ok){$('message').textContent=await r.text();return}location.href='/status'};document.onclick=async e=>{const b=e.target.closest('[data-a]');if(!b)return;if(b.dataset.a==='edit')edit(b.dataset.id);if(b.dataset.a==='delete'){await fetch('/api/admin/monitors/'+b.dataset.id,{method:'DELETE'});load()}};load();`;}
export function createServer(db:Database.Database,onChanged:()=>void,runMonitor:(m:MonitorConfig)=>Promise<CheckResult>,onHeartbeat?:(monitor:MonitorConfig,transition:{recovered:boolean;outageStarted:number|null})=>Promise<void>){const app=express();app.disable('x-powered-by');app.use(express.json({limit:'20kb'}));const auth=(req:express.Request,res:express.Response,next:express.NextFunction)=>{const u=process.env.ADMIN_USERNAME,p=process.env.ADMIN_PASSWORD;if(!u&&!p)return next();const h=req.headers.authorization;if(!h?.startsWith('Basic ')){res.set('WWW-Authenticate','Basic realm="Uptime Monitor Admin"').sendStatus(401);return;}const s=Buffer.from(h.slice(6),'base64').toString(),i=s.indexOf(':');if(i<0||s.slice(0,i)!==u||s.slice(i+1)!==p){res.set('WWW-Authenticate','Basic realm="Uptime Monitor Admin"').sendStatus(401);return;}next();};app.get('/api/status',(_q,r)=>r.set('Cache-Control','no-store').json(dashboardData(db,true)));app.get('/status',(_q,r)=>r.set('Cache-Control','no-store').type('html').send(publicPage(dashboardData(db,true))));app.post('/api/heartbeat/:token',async(q,r)=>{const t=receiveHeartbeat(db,q.params.token);if(!t)return r.sendStatus(404);if(onHeartbeat)await onHeartbeat(t.monitor,{recovered:t.recovered,outageStarted:t.outageStarted});r.json({ok:true,receivedAt:Date.now()});});app.use(['/dashboard','/api/admin'],auth);app.get('/dashboard',(_q,r)=>r.type('html').send(adminPage()));app.get('/api/admin/monitors',(_q,r)=>{const base=`${_q.protocol}://${_q.get('host')}`;r.json(dashboardData(db,false).map(m=>({...m,heartbeatUrl:heartbeatUrl(db,m.id,base)})));});app.get('/api/admin/export',(_q,r)=>r.json(dashboardData(db,false)));app.get('/api/admin/export.csv',(_q,r)=>r.type('text/csv').send('name,url,group,state\n'+dashboardData(db,false).map(m=>[m.name,m.url,m.group,m.state].map(v=>`"${String(v??'').replace(/"/g,'""')}"`).join(',')).join('\n')));app.post('/api/admin/backup',async(_q,r)=>{if(!process.env.BACKUP_DIR)return r.status(400).json({error:'BACKUP_DIR is not configured'});r.json({file:await backupDatabase(db,process.env.BACKUP_DIR)});});app.post('/api/admin/monitors',(q,r)=>{try{const m=validateMonitor(q.body,`monitor-${randomUUID()}`);const saved=saveMonitor(db,m);onChanged();r.status(201).json(saved);}catch(e){r.status(400).send(e instanceof Error?e.message:'Invalid monitor');}});app.put('/api/admin/monitors/:id',(q,r)=>{try{const old=getMonitor(db,q.params.id);if(!old)return r.sendStatus(404);const saved=saveMonitor(db,validateMonitor(q.body,q.params.id));onChanged();r.json(saved);}catch(e){r.status(400).send(e instanceof Error?e.message:'Invalid monitor');}});app.delete('/api/admin/monitors/:id',(q,r)=>{removeMonitor(db,q.params.id);onChanged();r.sendStatus(204);});app.patch('/api/admin/monitors/:id/active',(q,r)=>r.json(setMonitorActive(db,q.params.id,Boolean(q.body?.active))));app.patch('/api/admin/monitors/:id/maintenance',(q,r)=>r.json(setMaintenance(db,q.params.id,Boolean(q.body?.maintenance))));app.post('/api/admin/monitors/:id/test',async(q,r)=>{const m=getMonitor(db,q.params.id);if(!m)return r.sendStatus(404);r.json(await runMonitor(m));});return app;}
